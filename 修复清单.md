# 🟡 工程修复清单（Fix Checklist）

> 用途：复审 Phase 1 + Phase 2 后发现的待修项。实现方按「改法（spec）」执行，完成后我按「验收标准」逐条复审。
> 不含临床数值决策——那些在 `临床确认清单.md`。
>
> 状态：⬜ 待办　🔧 进行中　✅ 已改待复审　☑️ 复审通过

---

## F1 ─ 清理混入真实档案的测试数据　【优先·拦路】

- **位置**：`people/WEI/health_data.json`（`records` 与 `processed_files`）
- **问题**：真实记录里混着 `test.jpg / test_diagnostic.jpg / test_verify.jpg / N1–N6.jpg` 等测试来源的日期。
- **影响**：新加的**日历热力图**会把这些假日子画成真实格子；评分、趋势、报告全被污染。给家属/医生看前必须清。
- **改法（spec）**：
  1. 人工核对哪些 `records` 来自测试图（对照 `processed_files` 与截图）。
  2. 从 `records` 删除测试日；`processed_files` 同步移除对应文件名。
  3. 测试一律改用「客户1（测试）」空档案。
- **验收标准**：WEI 的 `records` 仅含真实截图来源的日期；日历热力图无幻影天。
- **复审点**：核对删除后日期序列与 `processed_files` 一致；`last_updated` 已刷新。
- **依赖**：无。**这是数据操作不是代码改动，需你/家属确认哪些是测试日**——我不擅自删真实数据。
- **状态**：⬜

---

## F2 ─ 图表参考线（标称）与判读阈值（生效）不一致　【应做】

- **位置**：`dashboard_template.html` SpO₂ 图参考线（`需关注阈值` 系列，约 L1228）`data: records.map(() => thresholds.spo2_danger)`
- **问题**：参考线画在**标称** `spo2_danger`（COPD=88），但 flag 触发在**生效值**（88−2=86）。一个 87% 的点视觉上低于虚线、却无任何标记 → 用户困惑。
- **改法（spec）**：二选一，团队定一个：
  - 方案 A：参考线也画**生效值**（`spo2_danger - spo2_precision`），与判读完全对齐；
  - 方案 B：保留标称线，但在图例/desc 加注「虚线为标称阈值，逐日判读已含 ±2% 容差」。
- **验收标准**：图上"低于虚线却不标记"或"标记却在虚线上方"的视觉矛盾消除，或有文字解释。
- **复审点**：确认所选方案在 PC 与手机两种宽度下都不产生新的误读。
- **依赖**：无（与具体数值无关，纯展示一致性）。
- **状态**：⬜

---

## F3 ─ 三视图风险措辞统一　【应做】

- **位置**：`dashboard_template.html`
  - `assessDay`（约 L1379–1381）：danger="血氧**偏低**…建议临床复测"、warn="血氧**留意**"
  - `calcScores` 徽章（约 L699–707）：需关注 / 留意 / 正常
  - 日历 `calLabels`（约 L1468）：正常 / 留意 / 需关注
- **问题**：同一 danger 档，一处叫"需关注"、一处叫"偏低"，三视图措辞不统一。
- **改法（spec）**：统一档位词为 **需关注 / 留意 / 正常**；danger 文案改"血氧**需关注** {v}%"。"建议临床复测/非诊断"作为后缀说明，不混入档位名。
- **验收标准**：徽章、逐日标记、日历 tooltip 三处对同一天给出**同一档位词**。
- **复审点**：抽 1 个 danger 日、1 个 warn 日，跨三视图核对措辞一致。
- **依赖**：无。
- **状态**：⬜

---

## F4 ─ 落实引用措辞修正（FDA → ISO 等）　【内容·依赖临床】

- **位置**：报告/页面中的依据标注（`report_generator.py` 附录、`dashboard_template.html` 提示文案、本仓 `walkthrough.md`）
- **问题**：见 `临床确认清单.md` C11（FDA 510(k) 错位）。
- **✅ 已解锁**：依据文档已给定替换措辞，可执行。
- **改法（spec）**：
  - "FDA 510(k) ±2-3%" → "医疗用途脉搏血氧仪按 **ISO 80601-2-61 / FDA 性能测试 Arms** 评价；具体设备以说明书/注册信息为准"。
  - 消费级腕式一律写 **"腕式参考·非诊断"**，不得写"FDA 510(k) 清关精度"。
- **验收标准**：全仓 grep `FDA 510|AASM参考`，0 残留；三处措辞一致。
- **复审点**：同上。
- **依赖**：无（措辞已定）。
- **状态**：⬜（可执行）

---

## F5 ─ 长期/移动展示的延后项确认　【需求澄清，非缺陷】

- **背景**：Phase 2 已落地日历热力图；Phase 3+4 又补了 `dataZoom` + 时间窗口选择器（7/30/90/全部）+ 移动端断点。长期"一眼看趋势"的痛点已基本解决。
- **Phase 3+4 已完成**：
  - [x] 折线图 `dataZoom`（手势缩放 + >30天滑块）
  - [x] 时间**窗口**选择（近7/30/90天/全部）
- **仍未实现**（确认是有意延后还是补做）：
  - [ ] 日 / 周 / 月**聚合粒度**切换（窗口≠聚合：现在长跨度仍逐日画点，没有按周/月取均值降密度）
  - [ ] 长跨度**服务端预聚合 + 改 API 取数**（现为全量 JSON 内联进 HTML，数据增长后 HTML 膨胀、手机变慢）
  - [ ] 日历 `orient:'horizontal'` 在**多年数据**下横向溢出、手机需横滚
- **触发时机**：365+ 天后才明显。
- **需你决策**：以上是否本期延后？若延后，建议在 `task.md` 标 backlog 并设触发条件（如"记录数 > 180 天再启动"）。
- **状态**：🔧 部分完成（dataZoom+窗口已做；聚合/服务端取数待决策）

---

# ═══ Phase 5：复审 Phase 3+4（时间窗口/dataZoom/日历）后的新发现 ═══

> 背景：Phase 3+4 的渲染改动**部分通过 Python 脚本把一大段 JS 注入到 `</script>` 之前**，
> 由此引入了一条与初始渲染**并行且已经发散**的渲染路径。以下按优先级排列。
> 优先级定义：**P0 = 用户立即可见的错误，必修**；**P1 = 明显体验缺陷**；**P2 = 技术债/小瑕疵**。

---

## F6 ─ 出现第二条且已发散的渲染路径　【P0·最高优先】

- **位置**：`dashboard_template.html`
  - 初始渲染（顶层脚本，模板字符串风格）：摘要卡 / 风险列表 / 数据表
  - 时间窗口点击处理器（约 **L1846–1909**，内联字符串拼接、简化版）**又把同样三块各实现了一遍**
- **问题（已实际发散，非隐患）**：两套 HTML 结构不一致。例：初始风险面板有一行 `🫁 血氧标记日…`（约 L1423），注入版头部（约 L1881）**没有**。
- **用户可见后果**：**点任意时间按钮 → 风险面板版式突变、"血氧标记日"那行消失**；今后任何对卡片/表格/风险列表的修改都要改两处，必然继续发散。
- **改法（spec）**：
  1. 抽一个统一渲染函数 `renderViews(filtered, label)`，内部负责：摘要卡（含环比箭头）、风险列表、数据表、并 `setOption` 更新各图表。
  2. **初始加载**与**时间按钮点击**都调用 `renderViews()`，删除注入处理器里重复的内联实现。
  3. 顺带消化 F9 的 null 高亮问题（见下）。
- **验收标准**：
  - 切换任意时间窗口后，风险面板/卡片/表格的**版式与初始加载完全一致**（含"血氧标记日"行）。
  - 全文件中摘要卡/风险列表/数据表的 HTML 模板**只剩一份**。
- **复审点**：搜索确认 `risk-assessment`、`summary-cards`、`data-table-wrapper` 的 innerHTML 赋值各只有一处来源。
- **依赖**：无。
- **状态**：⬜

---

## F7 ─ 个体化血氧阈值未传导到摘要卡颜色　【P1】

- **位置**：`dashboard_template.html` 摘要卡"平均血氧下限"的 `cls`
  - 初始版约 L595、注入版约 L1865，均为 `avgSpo2Min >= 95 ? 'trend-up' : 'trend-down'`（**写死 95**）
- **问题**：颜色判定用固定 95%，**没走 Phase 1 的个体化阈值**。WEI 平均血氧下限 ~91–92 → 摘要卡显示**红色↓**，但风险面板/徽章判**"正常"**。
- **用户可见后果**：**同一个人、同一指标，摘要卡红、风险面板绿，结论自相矛盾**。
- **改法（spec）**：摘要卡血氧 `cls` 改用个体化生效阈值，例如 `avgSpo2Min >= effWarningDisplay ? 'trend-up' : 'trend-down'`（与逐日判读同源）。
- **验收标准**：WEI 的血氧摘要卡颜色与风险面板结论一致（均"正常"绿）。
- **复审点**：确认普通人群（warning=95→生效93）下逻辑也合理。
- **依赖**：无（与 F6 同文件，建议合并在统一渲染函数里一起改）。
- **状态**：⬜

---

## F8 ─ 移动端把时间选择器整个隐藏了（自相矛盾）　【P1】

- **位置**：`dashboard_template.html` 约 **L491**，最小断点（480px）内 `.time-selector { display: none; }`
- **问题**：时间窗口选择器是本轮为"长期 + 移动端"做的**核心控件**，却恰恰在小屏手机上被 `display:none`。
- **用户可见后果**：在最该用它的设备（手机）上，**用户无法切换时间窗口**。
- **改法（spec）**：小屏不要隐藏；改为**换行（flex-wrap）或横向滚动**保留可用；按钮可缩小但必须可见可点。
- **验收标准**：480px 宽度下时间按钮组仍可见、可点、可切换。
- **复审点**：真机或 DevTools 360–414px 宽度下逐按钮验证。
- **依赖**：无。
- **状态**：⬜

---

## F9 ─ dataZoom 缩放状态跨窗口切换残留　【P2】

- **位置**：时间窗口点击处理器内的 `setOption`（约 L1846+），更新对象**未带 `dataZoom`**。
- **问题**：用户先在某图缩放（如拖到 50–100%），再点时间按钮切换数据集，旧的缩放百分比会**沿用到新数据**上，显示成一个莫名其妙的子区间。
- **用户可见后果**：切窗口后图表只显示一截、且与所选"近N天"不符。
- **改法（spec）**：处理器的 `setOption` 里对每个图重置 `dataZoom: [{ start: 0, end: 100 }, ...]`。
- **验收标准**：任意缩放后切换时间窗口，图表都从该窗口的完整区间重新显示。
- **依赖**：建议并入 F6 的统一渲染函数一起改。
- **状态**：⬜

---

## F10 ─ 注入块可维护性（技术债，记录备查）　【P2】

- **位置**：注入的时间窗口 JS（约 L1846–1909）。
- **问题**：该段对所有中文/emoji 用 `\uXXXX` 转义（Python 注入所致），与全文件其余的字面 UTF-8 风格不一致；且当初是用 `rfind('</script>')` 注入的，**结构脆弱、难手改**。
- **影响**：**功能正确**（JS 转义等价），但后续维护成本高。
- **改法（spec）**：随 F6 重构时，把这段改写为与文件其余部分一致的字面 UTF-8、正常缩进的源码（重构后此项自动消解）。
- **验收标准**：统一渲染函数为可读源码，无大段 `\uXXXX` 转义串。
- **依赖**：随 F6 一并完成。
- **状态**：⬜

---

## F11 ─ 7日均值线删除后的清理　【P2·清理，非缺陷】

- **背景**：经用户确认，7日滑动均值线**有意删除**（此场景数据量小，无临床意义）。删除本身正确，但留有尾巴需收尾。
- **位置 / 改法（spec）**：
  1. `dashboard_template.html` **L660** `movingAvg()` 现已无任何调用点 → **删除该死代码函数**。
  2. `dashboard_template.html` **L1184** spo2 系列数组末尾的残留空行 → 清除。
  3. **文档同步**：`walkthrough.md` 与 Phase 3+4 总结中"✅ 7日滑动均值线 / 心率图+血氧图叠加黄色虚线趋势线"等表述 → 改为"按需求移除（无临床意义）"或直接删去，避免文档与代码不符。
- **验收标准**：全文件 grep `movingAvg` 仅 0 处（删干净）；walkthrough 无"7日均值"残留；hr/spo2 图视觉无空系列。
- **复审点**：确认删除 `movingAvg()` 后无其它隐式引用报错（已核：当前 0 调用点）。
- **依赖**：无。
- **状态**：⬜

---

# ═══ Phase 6：临床依据文档（v0.1）落地项 ═══

> 来源：`doc/程序临床依据说明_健康数据阈值与告警规则.docx`。以下为该文档逼出的程序改动。
> ⚠️ 全部血氧逻辑改动须落到**单一数据源**（`get_thresholds()` + 同步 `getThresholds()`），三处引用同改，避免发散。

---

## F12 ─ danger 档不再减精度容差（D-1/D-2 决策）　【P0·临床已决策】

- **决策**：血氧 **danger 档用指南标称值**（COPD 88%、通用 90%），**不减容差**；±2% 容差**仅用于"留意"档**。理由：避免掩盖严重低氧。
- **位置（三处，须同改）**：
  - `dashboard_template.html` `assessDay`（danger 判定）、SpO₂ 图参考线、`calcScores` 血氧徽章
  - `report_generator.py` `_calc_scores` 血氧段（`eff_danger`/`eff_warning`）
- **改法（spec）**：
  - danger 触发：`spo2_min < spo2_danger`（**标称**，不减 precision）。
  - 留意触发：`spo2_min < (spo2_warning - spo2_precision)`（**保留**容差）。
  - SpO₂ 图"需关注阈值"参考线改画在**标称 `spo2_danger`**（与判读一致；这也顺带修正 F2 的对齐方式）。
- **验收标准**：
  - COPD：`<88%` 需关注、`88–90%` 留意、`≥90%` 正常；通用：`<90%` 需关注、`90–93%` 留意。
  - 构造 86–87% COPD 数据 → **判为需关注**（不再静默）。
  - WEI（最低 90%）仍为 0 需关注/0 留意。
- **复审点**：三处 danger 公式一致、均无 `- precision`；留意档仍含容差。
- **依赖**：无（已决策）。
- **状态**：⬜
- **关联**：取代原 F2 的"方案A"，按此统一。

---

## F13 ─ 哮喘不应自动套用 COPD 88–92% 放宽　【P1·临床正确性】

- **位置**：`health_data_schema.py` `has_copd`（关键词含"哮喘"）、`dashboard_template.html` `getThresholds`（同关键词表）
- **问题**：文档明确——88–92% 依据是 **COPD/高碳酸血症风险**，**哮喘 ≠ 高碳酸风险**，不应自动套同一放宽。当前关键词把"哮喘"并入 → 哮喘患者被错误放宽血氧阈值。
- **改法（spec）**：把"哮喘"从"自动放宽 88–92%"的关键词集移出；放宽仅针对 COPD/慢阻肺/阻塞性肺/高碳酸风险。哮喘是否放宽、如何放宽，标"待临床确认"，默认走通用阈值。
- **验收标准**：仅含"哮喘"无 COPD 的档案 → 血氧用通用阈值（90/95）；含 COPD → 88/92。
- **复审点**：schema 与 JS 两处关键词表一致。
- **依赖**：无。
- **状态**：⬜

---

## F14 ─ 加入统一免责声明 + 提示语非诊断化（C15）　【P1·合规】

- **位置**：`dashboard_template.html`（仪表盘页脚/血氧区）、`report_generator.py`（报告末尾）、主页面提示
- **改法（spec）**：
  1. 采纳文档第八节**整段免责声明**（仅趋势参考、不诊断不治疗、列举影响因素、持续异常或伴气短/胸痛/发绀等请复测就医、勿自行调整氧疗/用药）。
  2. 现有提示语去诊断/治疗指令化：以"复测、关注症状、联系医生/急救"替代任何"自行调整"暗示。
  3. 腕式睡眠数据表述为**"参考趋势"**，不用于诊断睡眠障碍。
- **验收标准**：仪表盘与 Word 报告均含统一免责声明；无指示用户自行调整治疗的文案。
- **依赖**：文案可由法务/临床再确认，但默认采纳文档草案即可上线参考版。
- **状态**：⬜

---

## F15 ─ C5–C7 高风险逻辑专项测试（附录硬性要求）　【P1·测试】

- **背景**：文档把容差/门控定为最弱证据、最高风险，要求专项测试覆盖。
- **改法（spec）**：建测试用例（可放 `tests/` 或独立脚本）覆盖：
  - 持续 86–89% COPD（应判需关注/留意，验证 F12 生效）
  - 短期单日 <88%
  - 多天 <92%
  - 读数缺失 / 异常波动
  - 设备误差模拟（±2%）
- **验收标准**：上述场景断言通过；F12 改动有回归保护。
- **依赖**：F12 先落地。
- **状态**：⬜

---

## 复审总结锚点

| 项 | 类型 | 优先 | 依赖 |
|----|------|------|------|
| **F6 统一渲染路径** | 正确性/一致性 | **P0（最高）** | 无 |
| **F7 阈值传导到摘要卡** | 一致性 | **P1** | 建议并入 F6 |
| **F8 移动端时间选择器** | 移动可用性 | **P1** | 无 |
| F9 dataZoom 重置 | 体验 | P2 | 建议并入 F6 |
| F10 注入块可维护性 | 技术债 | P2 | 随 F6 消解 |
| F11 7日均值删除清理 | 清理（非缺陷） | P2 | 无 |
| **F12 danger 不减容差** | 临床决策·正确性 | **P0** | 无（已决策） |
| **F13 哮喘≠COPD放宽** | 临床正确性 | **P1** | 无 |
| **F14 免责声明/非诊断文案** | 合规 | **P1** | 无 |
| **F15 C5–C7 专项测试** | 测试 | P1 | F12 先做 |
| F1 清测试数据 | 数据 | ✅ 已完成 | — |
| F2 线/判读对齐 | 展示一致性 | 并入 F12 | 无 |
| F3 措辞统一 | 展示一致性 | 高 | 无 |
| F4 引用修正 | 内容 | 中（可执行） | 措辞已定 |
| F5 长期展示 | 需求澄清 | 待定 | 你决策 |

## 建议执行顺序

1. **F6**（统一渲染函数）—— 一次重构同时消化 **F7 / F9 / F10**，是本轮性价比最高、也最该先做的一项。
2. **F8**（移动端选择器）—— 独立小改，恢复手机可用性。
3. **F1**（清测试数据）—— 需你/家属先指认测试日，再由实现方删。
4. **F2 / F3**（展示一致性，Phase 1+2 遗留）。
5. **F4**（引用）等临床回 C11–C14 后再动；**F5** 待你决策是否本期做聚合/服务端取数。

> 实现方完成任一项后改状态为 ✅，并 @我复审；通过转 ☑️。
> ⚠️ 提醒：F6/F7/F9/F10 高度耦合在同一段渲染代码，**强烈建议作为一个整改批次一起做**，避免反复改动同一处。
