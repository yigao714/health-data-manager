# 🔒 协作规则

## 核心原则

**只修复明确的问题，不做额外"改进"。**

---

## 1. 修改范围控制

### ✅ 可以做
- 修复用户明确指出的 Bug
- 用户明确要求的功能修改

### ❌ 不可以做
- 未经用户同意，修改已有功能的逻辑（如：自动入库、按钮显示/隐藏）
- 未经用户同意，修改业务流程（如：确认流程、数据入库流程）
- "顺便"优化、重构代码
- 修改与当前问题无关的文件或函数

---

## 2. 修改前必须确认

在动手修改之前，必须：
1. **说清楚要改什么** — 列出具体文件和行号
2. **说清楚为什么改** — 解释根本原因
3. **说清楚改后效果** — 用户能看到什么变化
4. **等用户确认** — 得到明确的"同意"才开始改

---

## 3. 最小修改原则

- 只改必须改的代码，不多改一行
- 不改变现有的用户交互流程
- 不改变现有的数据处理逻辑
- 如果有多种修复方案，选最小侵入的

---

## 4. 测试与验证

- 修改后必须验证不影响现有功能
- 如果修改可能影响其他功能，必须提前告知用户
- 出现新错误时，先回滚再排查
- **本程序以手机端展示为主**：涉及仪表盘展示的改动，必须验证手机端（窄屏 ≤480px）显示正常
- 改动仪表盘模板后，必须在真实浏览器打开重新生成的报告，确认图表实际渲染、控制台无报错（不能只看语法通过）

---

## 5. 文件保护清单

以下文件的**业务逻辑**未经用户确认不得修改：

| 文件 | 保护内容 |
|------|----------|
| `app.py` | 用户交互流程（确认入库、丢弃等）|
| `app.py` | API 端点的请求/响应格式 |
| `extract_health_data.py` | OCR 提取和验证逻辑 |
| `health_data_schema.py` | 数据模型定义 |
| `health_data.json` | 用户数据（不得删除或篡改）|
| `dashboard_template.html` | 图表展示逻辑（**源文件**）|

> ⚠️ `dashboard_template.html` 是仪表盘**源文件**；`people/*/dashboard.html` 是由它生成的**产物**。修改图表展示逻辑必须改模板并重新生成产物——直接改产物会在下次生成时被覆盖。

### 阈值单一数据源约束（重要）

健康告警阈值在**两处**定义，修改时**必须同步**，否则前后端告警不一致：
- Python 端：`health_data_schema.py` 的 `get_thresholds()`
- 前端：`dashboard_template.html` 的 `getThresholds()`

---

## 6. 错误处理

- 遇到网络错误（如 SSL、代理问题），先判断是环境问题还是代码问题
- 不要把环境问题当成代码问题来"修复"
- 不确定时，先报告分析结果，不要直接改代码
