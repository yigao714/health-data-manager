# 🩺 临床确认清单（Clinical Sign-off）

> 用途：以下阈值与引用都已写进代码（`health_data_schema.py` 的 `get_thresholds()` 与 `dashboard_template.html` 的 `getThresholds()`），
> 但**数值与医学出处必须由具备临床资质的人确认后才能给真实患者/家属使用**。
> 每条请填「确认 / 修改为 ___ / 待查」，并签名留痕。
>
> 状态图例：⬜ 未确认　✅ 已确认　✏️ 需修改　❓ 待查证

---

## 📌 依据文件与决策记录

- **依据文件**：`doc/程序临床依据说明_健康数据阈值与告警规则.docx`（v0.1，依据整理版，已核实公开指南）。该文件给出每条阈值的**证据强度分级**，但本身仍标"待临床确认"，不替临床签字。
- **证据强度速查**：
  - **较强**：COPD/高碳酸风险 88–92% 目标、SpO₂≤88% 严重低氧/LTOT 线 → 可作参考阈值。
  - **中等**：通用 <95% 留意 / <90% 需关注 → 可用但**须带非诊断声明**。
  - **弱/无直接依据**：减2%容差、原 COPD 86%、30% 门控、睡眠 5.5h、心率 50/45、步数 7000 → **只能作产品策略，须签字+风险接受记录**。

### ✅ 已决策（2026-06-10，用户/临床）
- **D-1（解决 C6）**：血氧 **danger 档保持指南标称值**（COPD 88%、通用 90%），**不再减精度容差**，避免掩盖严重低氧。
- **D-2（修订 C5）**：±2% 精度容差**仅用于"留意"档**，danger 档不用。
- **影响**：原"COPD 生效 86% 才需关注"的隐患消除 → 现 COPD `<88%` 即需关注、`88–90%` 留意；通用 `<90%` 需关注、`90–93%` 留意。WEI 实测仍为 0 需关注/0 留意（最低值 90%），但 86–87% 现在能被正确升级为需关注。

---

## A. 血氧（SpO₂）阈值

| # | 项目 | 当前取值 | 适用人群 | 需临床确认的问题 | 若错的风险 | 状态 |
|---|------|---------|---------|-----------------|-----------|------|
| C1 | 通用 danger | `<90%` 需关注 | 无 COPD/哮喘者 | 90% 作为"需关注"硬线是否合适？ | 偏高→漏报；偏低→误报 | ⬜ |
| C2 | 通用 warning | `<95%` 留意 | 同上 | 95% 作为"留意"下限是否合适？ | — | ⬜ |
| C3 | COPD danger | `<88%` 需关注 | 含"阻塞性肺/COPD/哮喘" | 88% 是否符合本院/本患者目标？ | 肺心病风险者漏报 | ⬜ |
| C4 | COPD warning | `<92%` 留意 | 同上 | 88–92% 目标区间是否照搬到本患者？ | — | ⬜ |

> 依据现状：BTS Emergency Oxygen Guideline（高碳酸血症风险者目标 88–92%）、GOLD Report。
> **章节定位未核实**，见 D 区。

---

## B. 精度容差与告警门控（本次新增逻辑，重点确认）

| # | 项目 | 当前实现 / 决策 | 需临床确认的问题 | 状态 |
|---|------|---------|-----------------|------|
| C5 | 精度容差 | **已决策 D-2**：±2% 容差**仅用于"留意"档**；danger 档用标称值 | 容差仅用于留意档是否认可？（留意档仍会少报，属低风险档） | ✏️ 待确认（方向已定） |
| C6 | 叠加效应 | **已决策 D-1 解决**：danger 不再减容差 → COPD `<88%` 即需关注，**不再下探到 86%** | 确认接受 COPD 88% / 通用 90% 作为需关注硬线 | ✏️ 待确认（隐患已消除） |
| C7 | 告警门控 | 30% 门控仅作"徽章升级"产品规则，**不挂指南名** | 30% 比例是否认可？（文档定性：无临床依据、属产品策略） | ⬜ |

> ✅ 文档与 D-1/D-2 决策后，"持续 86–89% COPD 全程静默不报"的隐患**已消除**（danger 现在 `<88%` 即报）。
> C7 仍是纯产品策略，须临床知情；C5 留意档少报属低风险，确认即可。

---

## C. 其它指标阈值

| # | 项目 | 当前取值 | 需确认问题 | 状态 |
|---|------|---------|-----------|------|
| C8 | 老年睡眠下限 | 65+ 用 `<5.5h` 才报"睡眠不足" | 5.5h 作为不报警下限是否过宽？ | ⬜ |
| C9 | 静息心率 | 正常 50–80；β阻滞剂下限放宽到 45 | 92 岁 + 多病者的区间是否需个体化？ | ⬜ |
| C10 | 步数 | 偏低 `<3000`，达标 `≥7000` | 对失能/卧床高龄者，7000 达标线是否现实？是否改个体化目标？ | ⬜ |

---

## D. 医学引用核对（措辞/出处）

| # | 当前引用 | 问题 | 建议改法 | 状态 |
|---|---------|------|---------|------|
| C11 | "FDA 510(k) ±2-3%" | 消费级腕式血氧多**未经 510(k) 清关**，引用对象错位 | 改为 **ISO 80601-2-61 / 通用脉氧精度标准**，并注明"消费腕式实测常超此误差" | ⬜ |
| C12 | "BTS 2017 §8.13 / §10.1" | 章节号未核实 | 临床确认准确章节定位 | ❓ |
| C13 | "GOLD 2023 Chapter 5" | 章节号未核实 | 同上 | ❓ |
| C14 | "AASM参考"（旧标注） | 已在代码改为"腕式参考·非诊断" | 确认新措辞医学上无误导 | ⬜ |
| C15 | "建议临床复测"引导语 | 是否构成医疗建议/免责风险 | 确认措辞或加统一免责声明 | ⬜ |

---

## E. 签字

| 角色 | 姓名 | 日期 | 备注 |
|------|------|------|------|
| 临床确认人 | | | |
| 复核（本系统） | | | 确认后回写到 `get_thresholds()` 注释，去掉"待临床确认" |

> 确认完成后：① 把最终数值回写到**单一数据源** `get_thresholds()`；② JS `getThresholds()` 同步；③ 删除代码里的"（待临床确认）"注释；④ 通知我做一致性复审。
