# 📊 仪表盘手机微信端完美展示与共享指南

本指南将为您详细介绍如何通过**云端发布**和**局域网共享**，解决苹果（iOS）手机在微信中打开 HTML 文件只显示源代码、以及华为手机不显示 ECharts 图表的问题。

---

## 💡 为什么手机微信打不开 HTML 文件？

在微信/企业微信中，直接发送 `dashboard.html` 文件：
* **苹果手机 (iPhone)**：微信将 `.html` 识别为普通文本文件，使用内置的文本预览器打开，因此**只显示网页源代码**。
* **华为等安卓手机**：虽然能渲染出网页界面，但微信的文件预览器出于安全策略**禁用了 JavaScript 脚本**，导致依赖 JS 的 ECharts 动态图表无法加载，呈现空白。

**解决方案**：
微信的内置浏览器对 **`http://` 或 `https://` 的网址链接** 拥有完整的网页渲染和 JavaScript 支持。只要把网页发布到云端或局域网，通过**网址链接**分享给手机，所有手机均可在微信中完美、动态交互地浏览仪表盘！

---

## 🛠️ 方法一：一键发布到云端 (Netlify) —— 🌟 推荐

此方案使用 Netlify 提供的免费静态网页托管服务。发布后，您会得到一个专属的网址，可在任何地方的任何手机上无限制访问，且无需保持您的电脑开机。

### 第一步：注册 Netlify 账号（只需注册一次）
1. 打开浏览器，访问 Netlify 官网：[https://app.netlify.com/signup](https://app.netlify.com/signup)
2. 推荐选择 **Email**（邮箱）注册，或直接使用 GitHub/GitLab 账号登录。
3. 按照页面提示完成邮箱验证和初始问答即可（完全免费，无需绑定信用卡）。

### 第二步：生成 Netlify 访问令牌 (Personal Access Token)
您的软件需要使用此令牌来帮您安全、自动地将报告上传到您的 Netlify 账户下。
1. 登录 Netlify 后，直接访问令牌管理页面：
   [https://app.netlify.com/user/applications#personal-access-tokens](https://app.netlify.com/user/applications#personal-access-tokens)
2. 在 **Personal access tokens** 区域下，点击右侧的 **"New access token"** 按钮。
3. 在弹出的表单中输入一个容易辨认的名称，例如：`health-report`，过期时间选择默认（或根据需求设置）。
4. 点击 **"Generate token"**。
5. **【关键】** 页面将展示一串长字符（如 `nla_xxxxxxxxx...`）。**请立即复制并妥善保存它**（该页面关闭后将无法再次查看此 Token，如果遗失只能重新生成）。

### 第三步：在软件中一键配置并发布
1. 双击运行您目录下的 `start.bat` 启动系统，并在浏览器中打开管理界面。
2. 导入数据后，在左侧选择对应的人员，并切换到右上角的 **"📊 仪表盘"** 选项卡。
3. 点击图表上方的 **"📤 发布到云端"** 按钮。
4. 界面上会弹出一个框：**🔑 首次使用需配置 Netlify Token**。
5. 将您刚才复制的 Netlify Token 粘贴到输入框中，然后点击右侧的 **"💾 保存"**。
6. 系统会自动保存 Token，并立即开始部署您的页面：
   * **首次发布**：系统将为该成员在 Netlify 上自动创建一个独立的站点（名称为随机生成的 `health-rpt-xxxxxx`）。
   * **后续更新**：再次点击 **"📤 发布到云端"** 时，系统会识别出已存在的站点并直接更新内容，速度极快（增量更新，未修改的文件不重复上传）。

### 第四步：复制链接，发送到微信
1. 发布成功后，界面将显示 **"✅ 已发布到云端"**。
2. 输入框中会出现类似下面的专属链接：
   `https://health-rpt-ab12cd34.netlify.app`
3. 点击右侧的 **"📋 复制链接"**，然后在电脑端微信/企业微信中，直接粘贴发送给该成员。
4. 成员在手机上点击此链接，无论 iPhone、华为还是其他安卓手机，都能在微信中直接打开，并且**ECharts 图表动效、交互筛选完全正常**。

---

## 📡 方法二：局域网快速共享 —— 🏠 适合同 WiFi 临时演示

如果您不想注册 Netlify 账号，或者不需要发给远程用户，只希望在家里、办公室内把页面快速展示给身边的人看，可以使用此方法。

### 使用步骤：
1. **确保手机和电脑连接了同一个 WiFi（或在同一个局域网络内）**。
2. 打开系统，选择对应人员，进入 **"📊 仪表盘"** 选项卡。
3. 点击上方的 **"🔗 局域网访问"** 按钮。
4. 下方会展开展示出形如以下的局域网地址：
   `http://192.168.1.100:8080/dashboard.html?person=张三`
5. 点击 **"📋 复制"** 按钮，将此链接在微信中发给自己或身边的人。
6. 对方在手机微信中点击此链接，或者复制到手机浏览器中，均可直接打开。

---

## ❓ 常见问题与限制说明

### Q1：Netlify 方案发布后，我的电脑关机了，手机还能打开链接吗？
**答：完全可以。** 
网页一旦发布成功，文件就会托管在 Netlify 的全球云服务器（CDN）上。您的电脑可以随时关机，手机端访问不受任何影响。

### Q2：访问速度如何？会容易出错吗？
**答：稳定、流畅。**
* Netlify 的 CDN 在全球都有节点。国内手机用户首次打开链接大约需要 3 ~ 8 秒（视网络情况而定，需要加载 ECharts 基础库）。
* 再次打开或页面刷新时，由于浏览器缓存机制，通常在 1 ~ 3 秒内即可秒开，体验非常丝滑。
* 平台稳定性极高，除非触发 Netlify 的免费额度限制，否则绝不会出现报错或无法访问。

### Q3：免费版的 Netlify 有什么限制？需要付费吗？
**答：普通个人用户完全够用，完全不需要付费。**
Netlify 的免费计划 (Starter Plan) 包含：
* **流量额度**：每月 100 GB 流量。以单个仪表盘约 1.1 MB 计算，每月可被点击访问约 9 万次！
* **站点数量**：单个账号最多可创建 500 个独立站点（即可以支持 500 个不同人的专属链接）。
* 如果流量超额，只会暂停访问，不会扣费，下个月会自动恢复。

### Q4：其他人能看到我发布的健康数据吗？安全性如何？
**答：安全性很高，无需担心隐私泄露。**
* 随机分配的网址（如 `health-rpt-ab12cd34.netlify.app`）包含 8 位随机字符，其他人无法猜测到此地址。
* Netlify 会默认对站点进行 "No-Index" 设置（通过 robots 协议禁止搜索引擎抓取），因此百度、谷歌等搜不到您的链接。
* **只有拿到您分享的专属网址链接的人才能访问**。

### Q5：数据更新了，我需要重新发新链接给用户吗？
**答：不需要。**
当您在电脑软件中更新了数据后，再次点击 **"📤 发布到云端"**，系统会将最新的仪表盘内容覆盖上传到**同一个站点**。
对于接收人来说，**原链接保持不变**，他们只需要在手机上重新打开或刷新该链接，就能看到最新的健康数据！
